# 2026-09-18 照片服务 v22：真实公网制作与交付

当前版本 `wechat-v22-photo-service`。以下将真实生图、规则转换、浏览器运行和部署状态分开记录；没有调用额外 AI 视觉质检、命名、修图或代码生成。

- 201 项通用测试、7 组独立服务假 worker 测试通过，资源检查与干净 Git index 导出构建通过。服务测试覆盖图片/像素限制、鉴权/CORS、所有权、幂等、单 worker、持久恢复、每日限额、秘密资产链接、确认失败最多一次重试，以及真实过期后正确口令续回原 owner。
- 首套预置灰猫使用 4 次 302.AI 图片请求生成设计和三阶段九宫格，共 27 个动作；规则转换后最终 PNG 保存在 `assets/custom-pets/gray-cat/`，9 伙伴构建为 8 内置加 1 灰猫。技术格式通过不等于美术语义审核通过。
- 独立真实全流程经用户授权，网页 HTTP 请求全程通过公开 Cloudflare HTTPS：上传灰猫→排队→本机异步 worker→4 次 302 图片请求（无重试）→固定规则裁切→返回 ready.pet→新伙伴免费出现在列表首位→刷新仍保存。原照片未作为公开素材提供。记录：`artifacts/dev-release-20260918/paid-flow/result.json`。
- 远程三个阶段图集实际加载，尺寸均为 1536×1536；显示及刷新验证记录：`artifacts/dev-release-20260918/paid-flow/display-result.json`。该新任务只归验收会话，没有替换随包灰猫或增加预置伙伴，公开构建仍为 9 只。
- 本轮付费图片请求合计为 8 次：首套预置 4 次＋独立真实全流程 4 次，不应把整个工作误记为只调用 4 次。fixture worker 自动测试不调用付费接口，也不计入这 8 次。
- 灰猫浏览器专项检查通过 81 个动作组合、6 次进化及 3 个视口；本机真实服务＋已生成素材 fixture 还验证真正过期的 401、同地址续期恢复旧任务、第二次制作、资产链接与多标签会话，报告在 `artifacts/web-client-20260918/qa-real/result.json`。
- dev 代码 `5922dde` 已成功构建并部署到 https://shidingz.github.io/math-game/dev/ ，工作流为 https://github.com/shidingz/math-game/actions/runs/35295976981 。首轮环境分支限制由用户修正；公开主站根 HTML 与 origin/main 字节相同。线上全新未登录浏览器验证 9 伙伴、三阶段图集、预填 API 地址和从真实 GitHub origin 到 Cloudflare 的匿名健康检查/CORS，零 JS 错误；未复制任何本地测试凭据。记录为 `artifacts/dev-release-20260918/public-result.json`。
- 本轮未完成新增微信原生/手机真机验收，没有微信审核或发布；没有支付、云端积分与成长账户系统。

以下保留旧版本历史记录，其中旧的“当前”或“未部署后端”仅对应当时版本。

# 2026-09-17 透明模板与容错裁切试装

## 2026-09-18 图形库扩充 v21

194项单测、assets/check/build、微信入口及共享逻辑哈希通过。32完整路径/20主题，19通用主题可达，自定义不会选警徽；相同色系有多种组合，跨阶段主题稳定、当前配色可变。图鉴160组（20主题×4等级×手机/桌面）与游戏104组（13宠物×4等级×手机/桌面）通过，前景无像素绘制，减少动态静止、无贴图/模型调用、升级/进化正常。12组真实来访阶段、等高/位置、延迟图集取消与存档不受影响通过。截图与结果在`artifacts/effect-library-20260918`。几何/背景/View/controller/访客/共享核心文件与v20哈希一致。主包1060KiB，总19.54MiB；未做微信真机或发布。

## 2026-09-18 线上原版主题风格 v20

191项单测、assets/check/build、微信导出/哈希和99684动作边界通过。线上网页已实际访问，4宠物×4等级截图及15个脚本哈希核对记录在artifacts/website-effects-20260918。新版104组13宠物×4等级×两视口主题效果通过，8个内置family各异，custom稳定；208次前景完全无绘制；无彩带/粒子贴图请求。普通升级、进化与等大双人来访回归通过。未操作用户原生模拟器，未做真机或发布。

## 2026-09-18 连续彩带与双人布局 v19

190项单测、assets/check/build、微信导出/哈希和99684动作边界通过。13宠物×4级×手机/桌面共104组，六种彩带均出现；无particle-art请求、无点状绘制，减少动态固定，普通升级/进化整带增强。双人12组当前实际阶段等高、左右不碰撞；正常离场逐步恢复原尺寸；延迟图集不提前缩小，取消与账本不变通过。artifacts/ribbon-smooth-20260918保存结果截图。未重新抢占用户操作中的微信原生工具，旧v18原生记录不代表v19真机或发布验收。

## 2026-09-18 彩带成长与伙伴来访

189项单测、assets/check/build、微信入口与共享逻辑哈希、99,684组动作边界通过。浏览器13宠物×9级×手机/桌面验证常驻彩带数量；5种视口检查界面、喂养和全屏升级；12组实际访客阶段及操作取消、锁定排除、延迟图片、存档不变通过。pet-geometry与已认可版本逐字相同。

微信开发者工具已实际显示新版v18画面并完成39张图集加载；原生点击打开列表成功，继续交互检查因用户同时试玩而停止，不把未完成的原生自动喂养/进化冒称通过。真实手机和发布未验收。详见artifacts/ribbon-worlds-20260918中的browser/native记录与work-log.json。

- `artifacts/sponge-alpha-20260917`：三阶段27姿势经`alpha-grid-v1`固定算法生成标准透明图集；实际输入模板也为Alpha透明PNG。兼容不同原始尺寸和偏移分格，不使用色键或AI修图。
- 151项普通测试、29项素材测试通过，涵盖位移格线、穿过名义切线的完整尾部、白/黑/绿主体、半透明细节、伪透明与粘连拒绝、多候选及离线恢复。旧8角色/344PNG的assets/check、网页build及微信导出检查通过。
- 5次302请求，4次返回图片，1次第三阶段上游429后同参重试；所有成功图均被冻结规则处理，未为本样例改裁切代码。四张原图禁止网络重切的图集哈希一致，7张成品PNG恢复一致。
- 8原角色＋4自定义，主包238KiB，总17.48MiB。375/390/1280验证最新伙伴首位免费、199/200兑换边界、独立成长、配色恢复、零余额无限喂养、27姿势、两次进化及正式存档隔离通过；15级程序特效通过。图库10图、9姿势、3种透明查看底色通过。
- 已查看实际透明图集、手机/桌面、喂食及进化截图。第二阶段有脸型、围巾配色偏差；原创替代未充分执行。仅接受本机试装，不代表全自动美术合格。
- 微信CLI打开新独立工程成功；本轮原生编译验收、真机、上传、审核、发布未完成，真实minigame-1未覆盖。

# 2026-09-17 固定模板完整伙伴试装

- `artifacts/ember-playable-20260917` 完成3阶段、27姿势，按 `guided-windows-v7` 固定格裁切与规则去底装配，没有AI修图或宠物专属代码。内框扩大到416×416，外格512×512；辅助线偏移可容忍，内框贴边标记检查，前景不因轻微跨内框而丢失。
- 150项普通测试、25项素材测试通过；资源清单仍为8个内置角色/344个PNG，check、网页build及微信导出检查通过。
- 独立测试包8原角色＋3自定义伙伴，主包236KiB、总16.32MiB。375/390/1280宽度通过新宠物首位/免费选择、199/200兑换边界、独立成长与持久配色、零余额无限喂养、27姿势、两次全屏进化、正式存档隔离。15级程序效果和嵌入进食图正常，无多余AI背景/特效/食物图片请求。
- 已查看实际图集与手机/桌面、喂食、升级截图；结果页10图和9种姿势切换正常。保留试装：初始形态未充分执行原创替代要求，局部仍有细小绿边/颗粒。不能把功能通过称为全自动美术或权利验收。
- 微信CLI成功打开独立工程；没有执行本轮原生编译验收、真机、上传、审核或发布。完整请求来源与失败记录见该目录 `generation-history.json`，人工查看记录见 `art-review.json`。

# 历史增补：空白格线模板实验（2026-09-16）

- 一次302.AI请求，输出保留了三格画框与辅助线；同宽高比标准化及辅助区检查通过。第一格底部31px未达到预设32px，固定检查拒绝。其他两格的只读像素诊断也显示贴框问题，未修改规则放行。
- `artifacts/guided-layout-study-v1` 保存实际3张输入、完整提示词、规则哈希、原始输出及拒绝记录；没有生成动作、导入宠物或新游戏包。
- 3项离线测试通过：不同长宽主体及标准化、空格/灰色主体越界/移框拒绝、九宫格去辅助区后与现有图集转换兼容。
- 实测仅支持“模板有助于稳定分格”的观察，不代表所有照片或动作成功率，也不代表自动美术验收。详细说明见GUIDED-PET-LAYOUT.md。

# 历史增补：Sunburst 小柴完整试装（2026-09-16）

- 复用已确认的Sunburst三阶段设计，新增3次动作生图；三张1536×1536九宫格经固定规则裁切/去底/对齐成27透明姿势。实际装配在禁止网络下运行成功，没有后处理AI调用。
- 147项普通测试、11项离线素材测试、资产清单/check、微信导出检查通过。新增测试覆盖302异步任务恢复、重复调用不再次POST、结果缓存、不确定提交不自动重发及错误文本凭据脱敏。
- `artifacts/shiba-sunburst-kit/wechat-test` 包含8原角色＋2本地宠物，主包231KiB，总计14.94MiB；默认无限积分，新小柴为默认伙伴，正式经济和存档隔离保持。真实minigame-1未被覆盖。
- 375/390/1280浏览器验证两个本地宠物的独立成长、重复导入、刷新恢复、三阶段27姿势、喂食和6/11级全屏进化通过。零余额无限喂食与正式规则隔离通过，无404和脚本错误。
- 新小柴Lv.1–15程序绘图、预设重载一致、进食不重复叠图通过；自定义素材仅下载头像和动作图集。已检查三阶段手机、进食与桌面升级截图。
- 微信CLI成功打开独立工程；本轮没有完成原生编译截图核验、真机、上传、审核或发布。候选仍为试装，生成图中后两阶段差异较细，不把尺寸/运行通过称为自动审美通过。

# 历史增补：角色模板与程序预设（2026-09-16）

- 暹罗猫三阶段及27姿势被规范为固定参考模板；新小柴使用Pro生成角色，背景、15级特效、进化特效、食物图标由共享Canvas代码绘制，没有相关单独生图请求。
- 正常四项角色生图，本轮因初期转换模块未就绪实际五次，已修复模块预检。最终选择第一阶段第二次输出；恢复任务保持此选择，不偷偷切换旧输出。真实缓存任务在禁止网络下重跑装配成功。
- 145项普通测试、11项离线素材测试通过，包括稳定预设、参数白名单、全部预设和15级绘图坐标、四次生成流程、重复装配、尺寸拒绝、抠底颜色保护。资源清单和check、微信导出检查通过。
- 独立工程 `artifacts/shiba-siamese-template/wechat-test` 包含8原角色＋新小柴；主包约229KiB，总计13.84MiB。默认无限积分且与正式存档隔离，未覆盖真实minigame-1。
- 375/390/1280浏览器验证新伙伴、27姿势、喂食、刷新存档、Lv.6/11全屏进化与正式规则隔离通过。另检查Lv.1–15程序特效、预设刷新稳定、进食不重复叠加食物；自定义宠物仅下载头像与三个动作图集，没有背景、特效、食物PNG请求。已检查手机截图。
- 生图并不保证像素级服从参考；技术验收只保证最终画布、固定裁切和边界。新图仍保留试装标记，不能将格式通过称为相似度或审美自动合格。未做本轮微信真机、上传、审核或发布，真实生成服务端尚未部署。

# 历史增补：Pro自动判断主体与无限积分测试（2026-09-16）

- 新照片使用Pro，无其他角色参考，共七项实际生图，没有额外视觉模型、AI修图或宠物专用代码。
- 三阶段27姿势、头像、食物、场景、四种特效经固定规则转换完成。真实结果使用禁止网络的预加载器重新运行缓存流水线通过；离线测试另验证重复装配的全部PNG哈希一致。
- 141项普通测试、9项素材测试通过。资源清单、check、网页build、微信导出检查通过。无限积分在测试模式允许120次零余额喂养，重复点击不重复成长；正式模式继续正常扣分。
- 独立测试工程 `artifacts/photo-pet-pro-20260916/wechat-test`：8原有伙伴＋小柴，主包224KiB、总计14.79MiB，保留来源AppID，未覆盖真实minigame-1。微信CLI已打开此工程。
- 375/390/1280宽度下，新宠物自动解锁、零余额喂养、刷新存档、27姿势、Lv.6/11全屏进化与正式存档隔离通过，无缺图或浏览器异常；已查看手机截图。
- 本轮未做视觉模型审核，报告只声称技术转换可用；未做微信真机、上传、审核或发布验收。真实上传/订单后端尚未部署。

# 之前增补：Seedream 双模型与多宠物分包（2026-09-16）

- Lite / Pro × 无参考 / 暹罗猫参考四版设定实际生成成功；两套完整候选各包含27姿势、头像、食物、场景和四种特效。Pro 第三阶段第四格通过官方 bbox 方法修复人手，仅合入目标格，其余八格保留。
- 139项普通单元测试通过，8项带 sharp 的离线素材/流水线测试通过。资源清单和 check 通过，原8角色344素材不变。
- 本地新宠物原本全部位于主包，两套素材即超4MiB；改为每只宠物独立分包，头像与动作加载前先等分包成功，失败可重试。
- 双新宠物构建：主包约225KiB、总计16.91MiB；安装版本保留原围巾小狗，共8原角色+3本地自定义伙伴，主包约227KiB、总计18.83MiB。构建保守预算通过，最终上传大小仍以微信工具统计为准。
- 两份构建均通过375/390/1280宽度的隔离浏览器验证：所有本地宠物导入、再次加入、重载、独立成长、3阶段×9动作、喂食与6/11级进化；无失败素材和浏览器异常。实际小屏截图已查看。
- 94个工程文件同步到 minigame-1，AppID和个人配置哈希保持；没有删除原小狗素材。工程文件备份 /private/tmp/minigame-1-before-dog-models-FcoDQI。安装后共享逻辑哈希、全部分包和无DOM入口检查通过。没有重写用户的微信存档。
- 结果页 artifacts/dog-evolution-study/review/index.html 的20张图片与2个动作选择器加载通过。图库仅展示本轮新结果。
- 视觉模型未全部通过：成长幅度有争议，另有将合理配饰遮挡判成错误等问题；完整候选保留 trial:true，不宣称无人值守质量验收合格。服务端、支付、资源托管尚未部署，本轮没有真机、上传、审核或发布验收。

# 独立整理版验收

## 微信小游戏增补（2026-09-15）

- 正式构建已安装到用户指定的 minigame-1，保留其 AppID 和 project.private.config.json；入口改为原生 Canvas 小游戏。
- 118 项单元测试通过；336 次浏览器触摸/动作检查通过，覆盖小屏手机、普通手机与桌面，8 角色三阶段、兑换、喂养、全屏升级、存档、后台暂停和分包失败重试。
- 安装后独立检查通过：真实 game.js 无浏览器对象可启动，24 张图集尺寸正确，核心逻辑与 math-game 哈希一致，正式环境不响应 test=1。
- 微信开发者工具 2.02.2608070 / 基础库 3.17.2 中实际启动并显示选宠物页面，原生触摸打开和取消领养确认弹窗；wx.loadSubpackage / wx.createImage 加载 8 个分包和 24 张图集成功。
- 主包 199449 bytes，运行资源总计 13171948 bytes（约 12.56 MiB），单角色最大约 2.70 MiB。最终上传统计由开发者工具确定。
- 尚未执行 iOS / Android 真机验收、上传、审核或发布。模拟器不能替代真机性能与低网速测试。

微信可重复执行检查：`npm run check:wechat -- <导出或安装目录>`。测试截图在临时 QA 目录，不加入源码或上传包。

## 原网页整理验收

验收日期：2026-09-14。测试从新项目根目录执行，没有引用旧项目输出、临时素材或旧 HTTP 服务。

| 检查 | 结果 |
| --- | --- |
| 现行单元测试 | 110 / 110 通过 |
| 角色与资源清单 | 8角色、344个PNG；文件存在、哈希、尺寸、帧索引、15级数据和500ms喂食检查通过 |
| 与整理前内容对照 | 407个保留的源码、素材、测试文件内容相同 |
| 运行包 | 独立生成98个运行文件，约82MiB；源码保留单帧，运行包排除单帧 |
| 浏览器 | 隔离Chrome上下文，390×844/DPR3手机和1440×900桌面 |
| 角色显示 | 8角色×3阶段×2尺寸全部加载，未出现资源404或页面脚本异常 |
| 玩法 | 首次任选宠物、测试赠分、正式与测试隔离、重复喂养、正确自动下一题、错误显示答案后继续通过 |
| 提示与布局 | 喂养提示在进度上方，不阻挡两个操作按钮；正式首页两尺寸无横向/纵向溢出 |
| 升级 | 全屏暗背景升级、角色展示、关闭返回通过 |

浏览器脚本 `scripts/browser-smoke.cjs` 可重复执行，截图和详细报告输出到 `artifacts/`，不提交 Git。dist 与 artifacts 均为可再生成文件，交付目录中不保留这些验收中间产物。

本次验证是网页Chrome桌面及手机尺寸仿真，未进行微信小程序编译或iOS/Android真机验收；后续微信工作需按迁移文档补测。
# 微信自定义伙伴验收增补

- 128 项单元测试通过，包含固定素材包验证、多宠物独立成长、完成幂等、上传重试、照片草稿恢复、无服务端模式、照片清理、非法/缺失任务ID与边界拒绝。
- 原微信浏览器验收通过336项交互/动作检查，覆盖8角色×3阶段、390/375/1280宽度、原有兑换喂食题库与升级流程。
- 新增隔离浏览器验收通过375/390/1280宽度：真实文件选择器保存两次照片记录、重载恢复、模拟10只自定义伙伴、重复完成去重、分页选择、独立喂养、6/11级进化；无浏览器脚本错误。截图已检查，测试素材使用既有比熊，不代表真实AI生成结果。
- `pack:pet` 对既有透明九宫格实际打包成功，输出头像、内容版本和每帧边界；素材清单更新及 `check` 通过；网页独立构建通过。
- 正式微信构建主包约218KiB、总约12.58MiB。已同步到 用户本机的 `minigame-1` 工程，保留AppID及个人配置；安装后入口、24图集和共享逻辑哈希检查通过，微信CLI成功打开工程。备份在本机临时目录，未提交。
- 新增功能的微信真机拍照/隐私授权、真实服务端登录上传、生图与下载、支付、云成长存档均未验收/未部署。CLI打开工程不等同于新增功能已通过原生编译、真机、审核或发布。此次未推送Git。

## 2026-09-16 本地素材接入

130 项单元测试通过；新增覆盖多个本地宠物、重复领取、独立成长、重启恢复、写入失败回滚和服务端编号冲突。原 8 角色/344 资源检查通过。实际第四版试装在 375、390、1280 宽度检查了本地领取、重复领取、喂食、重启、3 阶段 × 9 动作和两次全屏进化，无素材请求失败或浏览器异常。截图在忽略目录 artifacts/wechat-local。主包约 2000 KiB、总包约 14.32 MiB。浏览器验证不代表微信真机已验收。
## 2026-09-16 固定原创提示词实测

- 本次重新生成实际提交4次302.AI Sunburst请求：1次上游429，3张三阶段设计图，未调用动作生成或其他生图模型。记录在 `artifacts/fantasy-original-v1` 与 `artifacts/fantasy-original-v2`。
- v1文本未使初始形态摆脱输入角色的造型；通用模板更新为 `generic-original-v2`，对插画/虚拟形象默认重新构思身份，照片保留自然特征，所有输入使用同一文案。真实输出不能仅凭提示词声明视为原创或权利合格。
- v2两张图片为2172×724，均通过同宽高比标准化；固定裁切会分别截断第三/第二阶段，人工查看后拒绝入库。最新输出仅为设计预览，没有动作包、游戏导入或微信编译验收；转换门槛与切线未调整。
- 更新后的3项流水线离线测试通过，覆盖单图/多图提示词相同、规则哈希冻结、缓存离线转换、顺序请求及不重复POST。此检查不代表实际模型输出可用。

## 2026-09-17 兔子与共享场景

153项普通测试、29项素材测试通过；4次真实图片请求，0重试，0AI命名/视觉质检/修图。302.AI固定提示词与导出客户端31个模块已检查；网络限定用例通过。实际4张源图在禁止网络时重切，7张成品PNG缓存恢复均字节一致。

微信导出8内置＋5自定义，main1122KiB/total19.60MiB；check-wechat入口、共享逻辑、存档隔离通过。375/390/1280浏览器断言新伙伴首位免费、纯文字喂养按钮、独立成长、200积分边界、27姿势、6/11升级；15级共享背景与效果加载通过。结果页20张图片加载、9动作切换正常；没有外部API请求。微信CLI成功打开独立工程，不等于原生编译验收、真机或发布。遵照用户要求未做AI图片语义检查。

网页按钮同步修改后的既有浏览器回归也通过：8角色×3阶段×手机/桌面、喂食通知、做题、全屏升级与正式布局，无缺失资源。

## 2026-09-18 程序元素与手机布局

157普通测试、资源/check/build、check-wechat通过。13宠物×3阶段×全部姿势、跳跃及旋转共49,842组实际边界检查通过。320/375/390/430/1280屏幕下9个等级截图、控件范围、喂养、升级、减少动态偏好通过；独立测试存档的27姿势/进化/200积分/排序/存档回归通过。程序15级效果检查没有背景或特效图下载。本轮没有重生成宠物图片或调用AI质检。微信新版独立目录与说明见HANDOFF最新节，尚未原生编译/真机/发布验收。


## 2026-09-18 当前：自定义改名、逐级体型与星轨特效 v2

本轮按用户反馈只修改微信客户端与规则渲染，未修改宠物素材或生图提示词，未调用生成/视觉模型。首页名称下只保留等级，升级页面使用伙伴显示名；自定义伙伴默认「新伙伴」，点击名称/铅笔打开原生可编辑输入框，可保存1—12字的名字。`customPetNames`与素材包分开，按唯一ID保存，重新注册/素材更新不覆盖，正式与测试隔离；内置宠物名不可修改。名字显示统一到首页、列表、制作记录与升级页。

选择年级与范围后保存并回到主页，点击做题按钮才进入。相同范围保留未完成轮次，改变范围只清旧轮次，不提前出题。保持大展示框；Lv.1→15按0.53→0.98系数递增，三个阶段共用可容纳待机高度，避免换形态缩小。390×844测试兔子待机可见高度约157→290px；13只伙伴全部15级都逐级增大。宽姿势保留边界缩小，睡姿不放大。没有更改积分、成长、领养或兑换规则。

`growth-elements-v2`增加切面晶体、弯月、连线星座、双层星门、扇形光带和顶冠；Lv.6/11/15逐步出现，颜色来自宠物像素，ID稳定组合，运行时全部Canvas。仍无背景贴图请求；保留减少动态和喂养完成短绽放。说明见[CLEAN-GROWTH-EFFECTS.md](CLEAN-GROWTH-EFFECTS.md)。

独立导出`artifacts/pet-personality-20260918/wechat-test`，版本`wechat-v11-pet-personality`，8内置＋5自定义，主包262KiB，总18.76MiB。新版8787 `/wechat-test/preview.html`试玩、`/review/`成长对比；旧8786留作对照。161单元测试、assets/check/build、微信入口/哈希检查通过。99,684组实际角色动作边界通过；5屏幕×9等级的布局/喂养/全屏升级/减少动态，3屏幕×13宠物×15级的体型，以及改名取消/校验/重开/注册/名字独立/选题流程全部通过。正式经济回归（199/200积分、免费自定义、独立成长、刷新）也通过。审计未发现客户端模型地址、密钥或动态代码。测试结果位于同目录`core-tests.log`、`geometry-check.json`、`ui-qa/`、`interaction-qa/`、`economy-qa/`、`runtime-audit.json`。

微信CLI已打开独立工程，未覆盖minigame-1。原生手机输入法、真机性能与发布尚未验收；浏览器输入框是模拟原生API，不能写成已完成真机测试。后台、支付与云存档仍未部署，名字目前保存在本机。

## 2026-09-18 当前：56种自然元素、8类场景与紧凑宠物框

本轮新增`wechat/runtime/scene-elements.js`，56种独立Canvas图形，按天空气象、林间植物、花园田野、山川水域、地景奇观、宇宙星空、果园生活分7组；每项实际可画、在组合中可达，无占位图。完整列表见[NATURE-WORLDS.md](NATURE-WORLDS.md)。八种主题为森林、竹溪、果园、海岛、雪山、花野、星河、山谷。由像素色相选主题集合，宠物ID稳定选主题与配方，自然主体颜色保持可辨识，花果细节随宠物色相变化。6层场景，一级9景物+2飘动元素，最高17景物+6飘动元素，星门/光带沿用，随等级添景。全部在角色后面绘制，支持减少动态，零新图片、零生图或其他模型调用。

`pet-geometry.home`按角色三个阶段待机边界计算展示框，限制最大350逻辑px并适配矮屏，不再撑满屏幕余高；同一伙伴的框在成长过程中固定。兔子390×844样例从465px框收至321px，满级待机约276.5px，头顶约30px安全余量；各角色满级占框高度约86%—87%，保留跳起及完整轮廓。喂养/做题60→78px，范围/换伙伴44→52px，成长卡48→60px，底部32→40px。体型逐级增长、名字持久化、选题返回主页、积分/成长规则全部保持。

当前`growth-elements-v3`、导出`wechat-v12-living-worlds`，独立工程`artifacts/living-worlds-20260918/wechat-test`；8内置+5自定义，主包283KiB、总18.78MiB。8788 `/wechat-test/preview.html`试玩，`/review/`展示56元素、8主题，并可给同一宠物手动切换主题对比（仅开发页，游戏自动匹配）。旧8787留作对照。微信CLI已成功打开该独立工程，未覆盖minigame-1。

164普通测试通过；最终月亮轮廓与主题搭配调整后8项相关测试再次通过。资源/check/build、微信入口与共享哈希检查、99,684组实际角色姿势边界通过。5屏幕×9等级的布局、喂养、升级、减少动态检查通过；3屏幕的改名/存档/选题与13宠物×15级体型增长通过。8主题运行、56元素展示、开发页切换检查通过，静态调用面扫描无客户端模型地址、密钥或动态代码。结果在`core-tests.log`、`geometry-check.json`、`adult-fit.json`、`ui-qa/`、`interaction-qa/`、`worlds-qa.json`和`runtime-audit.json`。后者的四次生图任务是之前兔子生成的历史请求，本轮没有发出生成请求。

浏览器绘制测量仅为Canvas命令提交耗时，不能声称微信手机帧率达标。真机性能、输入法与正式发布尚未验收；后台/支付/云存档未部署。

## 2026-09-18 幻境光场＋元素觉醒与进化题数

`artifacts/elemental-awakening-20260918`：169项普通测试；assets/check/build；微信入口/共享哈希；99,684运动边界；5屏幕×9等级布局、喂养、升级和减少动态；3屏幕×13角色×15等级增长与改名/选题；实际正式模式答题+10、喂养扣分与6/11级进化估算；四类元素对比页均通过。已查看新版手机常态及正式提示截图。静态模型调用面审计通过，本轮无图片生成或额外模型请求。主包287KiB，总18.79MiB。CLI打开独立工程，未覆盖minigame-1；未完成原生编译、真机性能或发布验收。详细规则见[ELEMENTAL-AWAKENING.md](ELEMENTAL-AWAKENING.md)。

## 2026-09-18 奇幻世界56元素

`artifacts/kingdom-elements-20260918`，172普通测试、assets/check/build、微信入口/共享哈希、99,684动作边界通过。5屏幕×9等级验证共享图集加载、喂养、升级与减少动态；3屏幕×13角色×15级单调增长和改名/选题通过。56项素材全部组合可达，8场景及随机变体页面通过，无客户端模型请求。生成原图RGB棋盘已使用离线规则恢复Alpha，并用空隙分格修复自然图集偏移。已查看手机、场景对比、升级和素材图集。首次包体超限已通过共享图集压缩解决；真机/原生编译/发布未验收。
