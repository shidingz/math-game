# 交接：更新至 2026-09-18

## 2026-09-18 当前：v23 恢复经典网页布局

版本 `wechat-v23-classic-home`。浏览器 `/dev/` 首页以用户指定的原线上 `?test=1` 为参考：恢复主题浅色背景、伙伴卡片、气泡、成长条与桌面双栏；手机采用正常文档滚动，避免全页按高度缩小。`wechat/browser/classic-home.js` 只负责 DOM 首页和伙伴场景显示，制作/答题/伙伴列表等仍复用原控制器与 Canvas 操作页。照片伙伴、固定生成管线、制作地址、会话、存档键、兑换和成长规则未变。

宠物按当前阶段有效像素取景，保证宽体角色和动作完整；场景不再加载玩具世界底图。保留 v21 的 32 图形/20 主题、全屏升级、真实等级的等高双伙伴互动与自定义改名。相机/上传与服务设置为首页轻量入口，没有额外顶栏挤占画面。原根目录 CSS 将固定一屏规则排除 `.classic-web`，原入口视觉不变；微信原生布局仍保持原实现，新增取景参数默认关闭。JS/CSS/HTML 合成版本避免旧样式缓存。

203 单测、资源检查、普通构建和微信导出入口/共享逻辑哈希通过。375/390/1280 三视口检验 9 伙伴×3 阶段×9 动作，共 729 个可见边界组合；喂养保存、改名、范围选择返回首页、答题、照片文件选择、连接取消、全屏进化、等高互动与正式/测试隔离通过。截图与结果在 `artifacts/classic-layout-20260918/`；对照说明见根目录 `design-qa.md`。本轮未发起付费生图，未重新验证远程完整制作链路，未进行微信原生编译或手机真机验收。

## 2026-09-18 前一版：v22 照片制作服务与真实 Canvas 网页

版本 `wechat-v22-photo-service`。保留 v21 的背景和主题效果，不改成长、积分或已解锁伙伴互动规则。新增 `wechat/browser/` 真实浏览器适配层，以相机/相册文件选择、IndexedDB 持久原图、fetch 上传/轮询和 Canvas 游戏替代仅供模拟验收的 preview shim；根目录旧网页继续保留。新浏览器站目标为 GitHub Pages `/dev/`；dev 的 `5922dde` 已构建并发布到 GitHub Pages `/dev/`，线上页面和服务连通检查通过。

灰猫真实制作目录 `artifacts/gray-cat-release-20260918`，4 次 302.AI 图片请求生成三阶段设计及三张九宫格，得到三阶段 27 个动作。最终 PNG 与 manifest 已整理到 `assets/custom-pets/gray-cat/`，目录清单 `assets/custom-pets/catalog.json`；当前构建是 8 个内置伙伴加该灰猫，共 9 个伙伴，不包含所有历史实验角色。固定名「新伙伴」，可以在客户端改本机别名。技术规范检查与规则裁切完成，未调用额外 AI 视觉质检、命名或代码生成，不宣称自动美术语义审核通过。

`server/` 使用 Node 原生 HTTP、sharp、原子磁盘状态与单异步 worker，复用固定 Alpha pipeline。口令登录、按 owner 隔离照片和任务、幂等 requestId、最多三张 10 MiB 真解码图片、精确 CORS、请求/每日任务限制。原照片不公开；ready 行返回 `pet`，最终图片使用不可猜 token 的 HTTPS 链接。后台暂停/重启延续原 task id；仅明确终态 429/5xx 每步骤最多一次技术重试，不确定提交暂停，避免自动重复收费。

浏览器 token、过期标记和游戏库按完整服务地址隔离。真实过期后只暂停业务请求，保留 token 用于正确口令下的同地址续期；服务端可恢复原 owner 的排队/半上传任务。新设备或未知 token 不按 deviceId 接管已有 owner。收到任务确认后释放本机照片；生成多次得到不同 custom id 和独立进度。

新增命令 `pet:serve`、`test:pet-server`、`build:web-game`；依赖单独在 `server/package.json`，原 npm test/check/build 无安装依赖。`.env.local` 与 `.env.service.local` 是被忽略的私有配置，不放入站点或日志。本机 API 默认 `127.0.0.1:8799`，用户已授权 Cloudflare HTTPS 隧道，真实全流程已通过公开 HTTPS，详见下段；GitHub Pages `/dev/` 已发布并通过线上检查。没有支付、云端成长存档或微信原生身份联调；没有本轮微信真机/审核/上传发布结论。

主流程已报告 201 项通用测试与干净 Git index 导出构建通过。已完成的独立服务测试为 7 组假 worker HTTP 测试，包括真实过期后续期、重启、旧任务/半上传归属、幂等与资产访问；不触发收费 API。客户端 agent 已另做本机真实 server + 已有合格灰猫 fixture worker 集成：过期续接、多次领取、图集 URL 和多标签会话恢复，报告 `artifacts/web-client-20260918/qa-real/result.json`。该 fixture 测试与后续真实付费全流程分别记录，不能混淆。

真实网页全流程已通过公开 Cloudflare HTTPS：照片上传、排队、4 次 302.AI 图片请求（无重试）、固定规则裁切、免费加入列表首位和刷新保存；三个远程阶段图集实际以 1536×1536 加载。首套随包灰猫 4 次加本次真实验收 4 次，共 8 次图片请求，没有其他 AI 调用；新生成伙伴仅归测试会话，预置包仍为 9 个伙伴。 真实记录为 `artifacts/dev-release-20260918/paid-flow/result.json` 与 `display-result.json`，无需复制私有会话或资产 token 到文档。灰猫另有 81 个动作组合、6 次进化、3 个视口的浏览器检查全部通过。双源发布构建验证保留 main 原站字节；Pages `/dev/` 已发布且线上检查通过；微信真机仍未验收。最新集中记录见 [VALIDATION.md](VALIDATION.md)。

现行使用见 [LOCAL-PET-SERVICE.md](LOCAL-PET-SERVICE.md)、接口/恢复见 [server/README.md](../server/README.md)。以下为历史逐版记录，旧“后台未配置”“当前版本”等说法只表示对应日期版本，不能覆盖本节。

## 2026-09-18 前一版：保留v20风格，扩充32图形与20主题

用户认可当前风格，本轮只扩充特效图形库，版本`wechat-v21-effect-library` / `website-themes-v2`。新增`effect-motifs.js`，9种原图形+23种新图形=32种完整Canvas路径；`pet-particles.js`共20套主题，其中19套用于照片伙伴，盾徽保留内置柯基。自定义按首阶段色调+稳定ID选择兼容主题、图形组合、4摆位和3缓慢运动；每次仍2–5个完整图形，全部后层。内置8伙伴原主题/配色/顺序保持。修复初始灰白宠物后续变彩色仍锁旧配色的问题：主题锁首阶段，颜色取当前阶段。无新贴图、模型或网络调用。

背景、宠物常态大小、双人互动、积分成长/存档模块均与v20导出哈希一致。独立工程`artifacts/effect-library-20260918/wechat-test`，端口8797；`/review/`展示32图形、20主题、1–15等级、浅深背景与升级动画，默认新蝶舞主题；`/wechat-test/preview.html`试玩。8内置+5历史自定义伙伴，主包1060KiB，总19.54MiB。未覆盖真实minigame-1、未上传或发布。

194单测、assets/check/build、微信入口/核心哈希通过；图鉴20主题×4等级×2视口160组，游戏13宠物×4等级×2视口104组，来访12组实际阶段与取消/迟到图集/存档回归通过。已查看32图形及手机兔子蝶舞、小柴极光、全屏进化截图，无脚本/资源错误。验证仅浏览器与导出入口模拟，不冒称微信真机。详见[WEBSITE-EFFECTS.md](WEBSITE-EFFECTS.md)。

## 2026-09-18 前一版：取消彩带，参考线上原版主题特效

用户最终否定v19彩带，指定自己的线上网页作为风格参考。实际访问线上并查看4宠物4等级，8个角色特效源码与本地原版哈希一致。当前wechat-v20-website-effects/website-themes-v1采用雪晶、月牙、莲纹、祥云、盾徽等细轮廓主题图形，取消所有彩带和点状粒子；每次2–5个完整主题形状，全部后层，升级仅整体舒展增强。内置8主题各异；自定义伙伴按首阶段像素色调固定类别，后续随颜色调整。无AI调用。详见[WEBSITE-EFFECTS.md](WEBSITE-EFFECTS.md)。

保留v19等大双伙伴左右互动、原宠物大小、v18背景与全部当前经济/存档规则；不采纳线上旧的300积分或80题文案。独立工程artifacts/website-effects-20260918/wechat-test，8796 /wechat-test/preview.html，8内置+5历史自定义伙伴。191项单测、assets/check/build、微信入口/哈希与99684动作边界通过，主包1043KiB、总19.53MiB。未覆盖真实minigame-1、未上传发布。

## 2026-09-18 前一版：连续整体彩带、双伙伴等大互动

用户反馈串珠线条与碎点不好看。当前wechat-v19-smooth-ribbons/silk-v3使用连续填充的整条缎面曲面，六种轨迹按宠物ID稳定分配；所有粒子/移动高光点已移除，升级仅整带变宽变亮。1/2/3数量及三档成长保留，particle-art不再导出。无AI调用，复用v18背景。

访客图集就绪后，双方650ms过渡到左右等高布局，最大动作宽度决定公共contain尺寸；各自真实阶段不变，正常离场平滑还原。无访客时pet-geometry保持原样，主宠彩带随双人布局移动。190单测、资源/check/build、微信入口/哈希与99684动作边界通过。双人新增432组规则检查。

新独立工程artifacts/ribbon-smooth-20260918/wechat-test，8795 /wechat-test/preview.html，主包1040KiB，总19.52MiB。未覆盖真实minigame-1，未操作用户当前原生模拟器。旧v18原生验证不能冒称v19已完成真机或发布。详见[RIBBON-WORLDS.md](RIBBON-WORLDS.md)。

## 2026-09-18 前一版：三级彩带、清晰背景与伙伴来访

当前版本wechat-v18-ribbons-and-visits/flow-v2/toy-worlds-v3。彩带对全部伙伴1级常驻；6/11级增加数量为2/3条，各条有三档且档内逐级增强，进化保留旧彩带三档。普通点缀仅5–7个，提高饱和度与不透明度。pet-geometry.js与用户已认可版本完全相同，未修改宠物大小或经济成长。

背景恢复原生443px/格真实细节，新增8场景，总16场景室内外各8、48素材、6布局；少量侧边动景和低频顶部穿行。只预制共享图，不在运行时调用AI。来访伙伴严格已解锁、实际存档等级与阶段，一次一位，喂养/做题/升级/切换立即取消，延迟图集有令牌保护，不改经济。详细规则见[RIBBON-WORLDS.md](RIBBON-WORLDS.md)与[BACKGROUND-V3.md](BACKGROUND-V3.md)。

独立测试工程artifacts/ribbon-worlds-20260918/wechat-test，8794 /wechat-test/preview.html；8内置+5历史测试伙伴，主包1361KiB、总19.84MiB。未覆盖minigame-1。189单测、资产检查/构建、微信入口/哈希、99,684边界通过；234组彩带阶段、访客三阶段/取消/延迟资源/存档不变浏览器验收通过。原生微信工具验收脚本scripts/wechat-native-smoke.cjs，真实微信工具v18画面、原生列表点击与39图集加载成功，本轮native-readonly/report.json为只读验收；用户同时试玩，因此完整原生自动喂养/进化未跑完，已停止点击并恢复临时save拦截、未回滚用户手动进度。未进行手机真机与发布。

## 2026-09-18 前一版：粒子接入实际游戏，大小保持不变

用户反馈游戏中无特效：前版新效果只在预览，`kingdom-scenes.draw`是空函数。现在`view.js`使用`pet-particles.js`与`particle-effects.js`绘制32种预制Alpha粒子，按主色/种子选方案，随等级增强；6级流动光迹、11级加强环绕，普通喂养/升级与6/11进化都有明确星光爆发。旧Q版特效和光座继续停用。用户后续明确不放大，pet-geometry与上版完全一致。

4张最终Alpha图集在assets/particle-effects，提示词在docs/prompts/particle-effects。构建脚本离线预染8套色调，客户端不使用额外模型、离屏染色或代码生成。全屏升级不再绘制其下被覆盖的主页，减少重复粒子开销。版本wechat-v17-live-particles/flow-v1。

新独立测试工程artifacts/particle-live-20260918/wechat-test，8793 /wechat-test/preview.html，主包1329KiB、总19.81MiB，8内置+5历史测试伙伴；旧8792保留比较。未覆盖minigame-1，30/100题及旧存档规则不变。179单测、资源检查/构建、微信入口/哈希、99,684边界通过；手机与桌面检查常驻粒子、真实像素运动、Lv.2/6/11/16喂养升级爆发和素材请求通过，未请求模型接口。微信真机/原生编译与发布尚未验收。

## 2026-09-18 前一版：流动粒子预览、更大场景框与30/100题进化

用户废弃旧Q版特效和额外光座，保留背景并要求室内/室外各半。`kingdom-scenes.js`现在先按ID稳定地等概率分组，再组内按色相选择；13测试伙伴为7室内/6室外。`GROUPS`只保留32个背景/地面/道具/建筑元素，旧powers/motes/seals不再导出或加载，draw为退役占位，不绘制。版本`toy-worlds-v2`/`wechat-v16-roomy-evolution100`。

新增开发期独立粒子预览：4次imagegen预制32个Alpha粒子，程序计算12种动画，重点流动星河、璀璨星尘、追光流星、电弧。可按宠物颜色染色、调等级/浓度、换场景并暂停。`wechat/experiments/particle-effects.js`和本轮artifacts不进入游戏包；等待用户挑选后再集成。没有新宠物或额外AI命名/质检/裁切。

实际游戏扩大场景框，满级宠物低于框高75%，逐级体型保持。标准曲线改为累计300/1000/1500成长到6/11/15，Lv.15以后每级仍300；每题+10及20积分喂20成长不变。成长卡只提示下一次进化剩余题数，30题到6级、累计100题到11级，11后“已达终极形态”。新版growthVersion=2对旧曲线按比例迁移级内进度一次，保留等级、积分、伙伴和喂养次数。网页同步曲线与文案。

独立测试工程`artifacts/particle-study-20260918/wechat-test`（652KiB主包/19.14MiB总包），未覆盖minigame-1。8792 `/review/`为粒子比较，`/wechat-test/preview.html`为新布局与进化试玩。包含8内置+5历史试装自定义伙伴，构建明确允许试装。详情[PARTICLE-STUDY.md](PARTICLE-STUDY.md)。

175普通测试、assets/check/build、微信入口/哈希、99,684边界检查通过；5屏×9级布局/喂养/升级、3屏×13宠物×15级增长/改名/选题流程、正式钱包与6/11进化、网页两尺寸五等级文案、12粒子方案/32素材预览通过。手机/桌面截图已查看。37客户端模块审计无模型端点/密钥/动态代码。原生CLI只打开独立工程；本轮未验收微信原生编译、真机或发布，服务端/支付仍未配置。

初次3个单测因旧按钮尺寸和旧成长值断言失败，已按新布局和进度比例迁移更新，175项全部通过；初版光带太细，已加粗并增加发光核心，仍在独立预览供挑选。系统python3受本机Xcode许可限制，改用已捆绑Python，没有修改系统许可。

## 2026-09-18 前一版：56项Q版日常元素，实心立体特效

用户的新三张参考改为简洁圆润、有3D玩具体积的风格。内置imagegen预制7张图集，共56项：8个日常/魔法背景、8地台、8小道具、8建筑/车、8大特效、8粒子、8光座。最终素材`assets/toy-worlds/`，固定提示词`docs/prompts/toy-worlds/`；详见[TOY-WORLDS.md](TOY-WORLDS.md)。没有生成新宠物。

`kingdom-scenes.js`与`build-kingdom-art.cjs`沿用历史文件名，内容已换为Q版；版本`toy-worlds-v1`/`wechat-v15-toy-worlds`。背景包括游乐场、木屋、巴士站、星空屋、海滩、云屋、花园、车库。仍按宠物像素色调和ID稳定组合。常驻图片全不透明绘制（保留PNG本身边缘Alpha），星星/彩虹/云团等颜色浓郁；Lv.6大特效、Lv.11伴随大粒子、Lv.15星冠。减少动态、手机紧凑框、逐级体型、进化题数、改名、练习范围与全部经济规则保留。

六张原始抠图图集为RGB品红底，按提示词预先约定的备用格式用固定规则去底，没有调用AI检查/抠图。`prepare-toy-art.cjs`限制在背景邻域恢复边缘，避免内部紫色误去除，并自适应透明切线；专用于这套预制素材，不对任意用户照片承诺通用。旧王国素材留作历史，不随当前微信构建发出；重建会移除旧world-art。

独立测试工程`artifacts/toy-worlds-20260918/wechat-test`，8791 `/review/`可比较8场景/56元素/等级并“再组合一套”，`/wechat-test/preview.html`试玩。8内置＋5既有自定义伙伴；测试包含历史试装伙伴，构建显式`--allow-trial-pets`。主包880KiB、总19.37MiB，微信CLI已打开此独立工程，未覆盖minigame-1。

173普通测试、资源清单/check/build、微信入口/哈希、99,684动作边界通过；5屏×9等级布局/喂养/升级、3屏×13宠物×15级增长/改名/选题回归、8场景与56素材组合检查通过。首次并行交互测试等待图集超时，单独复测通过，未复现，尚未确定原因；增加测试诊断而未随意修改加载器。详细问题在本轮`work-log.json`。浏览器运行无模型请求；7次模型调用仅本次美术预制，审计中的4次宠物任务为历史兔子记录。

已查看手机和桌面截图。未做本轮原生编译、微信真机性能或上传发布。后台/支付/云存档仍未部署。


## 2026-09-18 前一版：56项奇幻插画元素、颜色约束的随机组合

用户提供六张参考，改为有空间层次的幻想场景与鲜明手绘战斗特效。内置imagegen预制7张图集，共56项：天空、地形、自然景物、建筑遗迹、觉醒轮廓、动态粒子、脚下法阵各8项。成品`assets/kingdom-elements/`，提示词`docs/prompts/kingdom-elements/`，详情见[KINGDOM-ELEMENTS.md](KINGDOM-ELEMENTS.md)。原始输出和过程只放本轮artifacts。无新宠物生成/命名/视觉模型调用。

默认渲染已换`kingdom-scenes.js`，`kingdom-kit-v1`/`wechat-v14-kingdom-kit`。按已有主色色相加权选场景与特效，ID种子稳定随机选择兼容侧景和粒子；八个场景基调。Canvas负责层叠、漂浮、缩放/旋转与喂养绽放，运行时不调用AI。6级大型觉醒轮廓，11级加强，15级封顶；旧v3/v4程序元素仍保留但不渲染。保留进化题数、紧凑框、体型、改名、练习范围和经济。

原图并未返回真正Alpha，而是画入棋盘；离线像素规则恢复透明，白色景物使用局部棋盘纹理检测以保留水晶/花瓣。自适应透明空隙分格修复自然素材上下两排不等分造成的串格。不要将此预制图集去底规则应用到任意用户照片。初次包体超过20MiB，改成224px透明景物、128px动态粒子与JPEG远景，保留256px格的源成品和全部13只宠物。

独立工程`artifacts/kingdom-elements-20260918/wechat-test`，8790 `/review/`展示56元素并可“再组合一套”，`/wechat-test/preview.html`试玩。微信CLI已打开独立工程，未覆盖minigame-1。172普通测试、资源/check/build、微信入口/哈希、99,684动作边界、5屏幕×9等级显示/喂养/升级、3屏幕×13宠物×15级体型和改名/选题回归通过。56项规则可达、8场景与变体比较通过，客户端无模型请求。审计中的4次宠物任务为历史兔子；本轮7次imagegen为共享场景预制。

少量素材边缘仍可能存在像素去底损失；共享插画并非无限分辨率。未验收本轮原生编译、真机性能或发布，后台/支付/云存档仍未部署。


## 2026-09-18 前一版：A幻境光场＋D元素觉醒、进化题数提示

用户选定A背景＋D特效，当前 `growth-elements-v4`、导出 `wechat-v13-elemental-awakening`。程序雾光背景替代自然场景，颜色按宠物像素匹配日焰/冰晶/风叶/星雷四类；6级环绕光环，11级大型元素轮廓与双环，15级光冠，喂养1秒绽放。所有效果在角色后方，支持减少动态，不使用背景贴图或AI。56种自然元素库保留但当前渲染不调用。

成长卡下显示Lv.6与Lv.11里程碑，以及当前成长和钱包计算的“还需答对N题＋喂养M次”；积分足够直接提示喂养，测试模式显示等价题数。零起点30题到6级、累计80题到11级，须将积分用于喂养；没有答题自动进化。11级以后提示两次进化已完成，继续成长。计算单独放 `evolution-progress.js`，不改积分/成长规则。卡高108，矮屏缩展示区保留78px按钮；名称、选题返回主页与逐级体型保留。

独立测试工程 `artifacts/elemental-awakening-20260918/wechat-test`，8内置＋5自定义，主包287KiB/总18.79MiB。8789 `/review/`对比四元素和等级，`/wechat-test/preview.html`无限积分试玩。未覆盖minigame-1。详情见[ELEMENTAL-AWAKENING.md](ELEMENTAL-AWAKENING.md)。

169普通测试、assets/check/build、微信入口及共享哈希、99,684组实际角色运动边界通过；5屏幕×9等级布局/喂养/升级，3屏幕×13宠物×15级体型及改名/选题回归通过。新浏览器测试实际正式模式两道答题＋喂养、钱包抵扣、6/11级进化提示和四种特效通过。已查看手机截图。运行时静态审计无模型地址/凭据/动态代码；报告中的4次任务为历史兔子图片请求，本轮零模型请求。依赖扫描首次因未指定NODE_PATH找不到sharp，指定捆绑依赖后通过，无运行时失败。

微信CLI已打开独立工程；本轮未验证原生编译、手机性能或发布。后台生成、支付、云存档仍未部署。



## 2026-09-18 前一版：56种自然元素、8类场景与紧凑宠物框

本轮新增`wechat/runtime/scene-elements.js`，56种独立Canvas图形，按天空气象、林间植物、花园田野、山川水域、地景奇观、宇宙星空、果园生活分7组；每项实际可画、在组合中可达，无占位图。完整列表见[NATURE-WORLDS.md](NATURE-WORLDS.md)。八种主题为森林、竹溪、果园、海岛、雪山、花野、星河、山谷。由像素色相选主题集合，宠物ID稳定选主题与配方，自然主体颜色保持可辨识，花果细节随宠物色相变化。6层场景，一级9景物+2飘动元素，最高17景物+6飘动元素，星门/光带沿用，随等级添景。全部在角色后面绘制，支持减少动态，零新图片、零生图或其他模型调用。

`pet-geometry.home`按角色三个阶段待机边界计算展示框，限制最大350逻辑px并适配矮屏，不再撑满屏幕余高；同一伙伴的框在成长过程中固定。兔子390×844样例从465px框收至321px，满级待机约276.5px，头顶约30px安全余量；各角色满级占框高度约86%—87%，保留跳起及完整轮廓。喂养/做题60→78px，范围/换伙伴44→52px，成长卡48→60px，底部32→40px。体型逐级增长、名字持久化、选题返回主页、积分/成长规则全部保持。

当前`growth-elements-v3`、导出`wechat-v12-living-worlds`，独立工程`artifacts/living-worlds-20260918/wechat-test`；8内置+5自定义，主包283KiB、总18.78MiB。8788 `/wechat-test/preview.html`试玩，`/review/`展示56元素、8主题，并可给同一宠物手动切换主题对比（仅开发页，游戏自动匹配）。旧8787留作对照。微信CLI已成功打开该独立工程，未覆盖minigame-1。

164普通测试通过；最终月亮轮廓与主题搭配调整后8项相关测试再次通过。资源/check/build、微信入口与共享哈希检查、99,684组实际角色姿势边界通过。5屏幕×9等级的布局、喂养、升级、减少动态检查通过；3屏幕的改名/存档/选题与13宠物×15级体型增长通过。8主题运行、56元素展示、开发页切换检查通过，静态调用面扫描无客户端模型地址、密钥或动态代码。结果在`core-tests.log`、`geometry-check.json`、`adult-fit.json`、`ui-qa/`、`interaction-qa/`、`worlds-qa.json`和`runtime-audit.json`。后者的四次生图任务是之前兔子生成的历史请求，本轮没有发出生成请求。

浏览器绘制测量仅为Canvas命令提交耗时，不能声称微信手机帧率达标。真机性能、输入法与正式发布尚未验收；后台/支付/云存档未部署。


## 2026-09-18 前一版：自定义改名、逐级体型与星轨特效 v2

本轮按用户反馈只修改微信客户端与规则渲染，未修改宠物素材或生图提示词，未调用生成/视觉模型。首页名称下只保留等级，升级页面使用伙伴显示名；自定义伙伴默认「新伙伴」，点击名称/铅笔打开原生可编辑输入框，可保存1—12字的名字。`customPetNames`与素材包分开，按唯一ID保存，重新注册/素材更新不覆盖，正式与测试隔离；内置宠物名不可修改。名字显示统一到首页、列表、制作记录与升级页。

选择年级与范围后保存并回到主页，点击做题按钮才进入。相同范围保留未完成轮次，改变范围只清旧轮次，不提前出题。保持大展示框；Lv.1→15按0.53→0.98系数递增，三个阶段共用可容纳待机高度，避免换形态缩小。390×844测试兔子待机可见高度约157→290px；13只伙伴全部15级都逐级增大。宽姿势保留边界缩小，睡姿不放大。没有更改积分、成长、领养或兑换规则。

`growth-elements-v2`增加切面晶体、弯月、连线星座、双层星门、扇形光带和顶冠；Lv.6/11/15逐步出现，颜色来自宠物像素，ID稳定组合，运行时全部Canvas。仍无背景贴图请求；保留减少动态和喂养完成短绽放。说明见[CLEAN-GROWTH-EFFECTS.md](CLEAN-GROWTH-EFFECTS.md)。

独立导出`artifacts/pet-personality-20260918/wechat-test`，版本`wechat-v11-pet-personality`，8内置＋5自定义，主包262KiB，总18.76MiB。新版8787 `/wechat-test/preview.html`试玩、`/review/`成长对比；旧8786留作对照。161单元测试、assets/check/build、微信入口/哈希检查通过。99,684组实际角色动作边界通过；5屏幕×9等级的布局/喂养/全屏升级/减少动态，3屏幕×13宠物×15级的体型，以及改名取消/校验/重开/注册/名字独立/选题流程全部通过。正式经济回归（199/200积分、免费自定义、独立成长、刷新）也通过。审计未发现客户端模型地址、密钥或动态代码。测试结果位于同目录`core-tests.log`、`geometry-check.json`、`ui-qa/`、`interaction-qa/`、`economy-qa/`、`runtime-audit.json`。

微信CLI已打开独立工程，未覆盖minigame-1。原生手机输入法、真机性能与发布尚未验收；浏览器输入框是模拟原生API，不能写成已完成真机测试。后台、支付与云存档仍未部署，名字目前保存在本机。


## 2026-09-18 前一版：清爽程序成长特效与手机放大

按用户反馈停用大幅幻想背景和预制能量环。新版微信首页与升级使用 `wechat/runtime/growth-effects.js`：八种Canvas基础元素（星芒、五角星、菱形、气泡、十字、叶片、短流星、圆环），按像素色相与稳定ID组合；每级增加半径、大小、透明度，6级加弧线和轨道光点，10级第二圈，11级第二弧与流星，15级顶部星芒。常态最多9点缀、2环、2弧、2流星；喂养/升级完成短绽放850ms。超过15级保持最终效果。内置角色导出时从各阶段待机像素统计颜色，自定义角色沿用存档配色。旧照片/场景素材保留历史，但新版不打包、不请求shared-art。整个本轮零生图/其他AI接口调用。

`pet-geometry.js`集中处理紧凑首页和自适应放大；按站立姿势设置主展示比例，宽动作可以缩小避免裁切，睡姿不额外放大。390px样例兔子初始可见高度232→328px（约42%），舞台330→465px。标题、进度、按钮压缩，但通知仍在宠物脚下、进度上方，主按钮60px、年级与换伙伴44px。沿用所有经济与存档规则。浏览器尊重减少动态效果设置。

独立输出 `artifacts/clean-growth-20260918/wechat-test`；8内置＋5自定义，主包255KiB、总18.76MiB。8786 `/wechat-test/preview.html`试玩，`/review/`交互切换等级和宠物、看前后对比与常态等级截图；旧8785保留。157单元测试、资源/check/build、微信导出检查通过；320/375/390/430/1280屏幕9等级、喂养、升级、减少动态测试通过，额外原有27姿势/积分/存档回归通过。真实全部13角色三阶段共49,842组姿势运动边界计算通过；15级效果不请求背景或特效图片。已查看UI截图确认紧凑布局和程序效果，不进行生成图片身份或解剖评审。本轮未修改生图提示词、宠物素材或调用生成流水线。

当前仍非正式发布，后台生成/支付/云存档未部署，未覆盖minigame-1。历史预制美术说明以下为历史状态，以本节和 `docs/CLEAN-GROWTH-EFFECTS.md` 为准。

## 2026-09-17 当前：兔子固定流水线与共享幻想场景

用户要求每只新宠物只调用第三方生图，不调用 AI 视觉质检、命名或改代码。真实任务 `artifacts/rabbit-alpha-20260917`：固定 `alpha-original-v1` 提示词未改，参考换成规则拼接的萨摩耶三阶段；4次302.AI图片请求全部首次成功，三阶段27姿势经现有 `alpha-grid-v1` 转换完成，未查看图像做AI语义验收。名固定「新伙伴」，ID `custom-dc290065be7b5f0e1b548394`。默认流水线自动生成唯一ID、固定名称、冻结参考图片哈希，状态区分技术通过与未做语义检查。新增 `pet-alpha-run.cjs` 自动轮询，明确终止429/5xx每步最多重试一次；未知POST或几何失败停止，归档请求纳入总次数。

按用户单独授权，开发期内置imagegen生成4张幻想背景＋1张透明能量环，原图在 `assets/fantasy-scenes/`；所有宠物复用，运行时零AI。根据宠物像素主色匹配金/绿/蓝/紫主题，浅色宠物背景适当压暗，预制环通过程序着色与运动；15级效果逐渐增强。微信喂养按钮取消食物图标并居中「喂养伙伴」，网页也去掉喂食卡片图标。旧本地试装标志保留构建门槛，名称不再追加“·试装”。

独立测试导出 `artifacts/rabbit-alpha-20260917/wechat-test`，8内置＋5自定义，主包1122KiB，总19.60MiB。新伙伴首位、免费、多存档、无限积分测试；本机8785 `/wechat-test/preview.html`，结果页 `/review/`。153普通测试＋29素材测试、资源/check/build、微信导出入口与哈希检查通过；375/390/1280浏览器断言27动作、喂食、6/11进化、200积分边界、排序与正式存档隔离；15级共享场景与效果加载通过。程序检查不代表已通过动作解剖或相似度评审。

默认生成与客户端审计见 `docs/RULE-ONLY-PET-FLOW.md` 和任务 `runtime-audit.json`。客户端无模型URL/Key或动态代码；旧Seedream与视觉审计命令未接默认流程、未进微信包。服务端尚未配置，支付、云端队列、真机、正式发布未完成。没有覆盖真实minigame-1。

## 2026-09-17 最新：透明PNG模板与容错分格完成

默认生成入口已换成 `pet-alpha-pipeline.cjs`（`pet:generate` / `pet:generate:alpha`），规范见 [ALPHA-PET-PIPELINE.md](ALPHA-PET-PIPELINE.md)。模板是带黑线的真正RGBA透明三格/九格PNG，请求同时设置`background=transparent`；提示词严格要求96/48px留白，但验收改为保留Alpha、识别长直辅助线、清理近透明噪点、在名义位置附近寻找透明间隙分行分列。允许格线偏移与返回尺寸差异，最终统一成512格、416×400最大主体和448足底。色键去底不再作用于这条路径，保留白/黑/绿主体和半透明细节；缺Alpha或相邻主体粘连会停止。分界接触主体与外边缘贴边标记检查，像素规则不保证语义完整。旧灰底流程保留`pet:generate:guided`，不改写旧任务。

真实样例 `artifacts/sponge-alpha-20260917`，ID `custom-tide-alpha-01`，名“泡泡伙伴”。三阶段27姿势、头像、配色和数据包全部生成并装入独立测试版；前3只本地自定义伙伴仍保留，共8内置＋4自定义，主包238KiB、总17.48MiB。本机8784：`/review/`查看素材、真实透明模板、实际提示词、浅/深/棋盘背景与记录；`/wechat-test/preview.html`玩无限积分测试。微信CLI已打开独立工程，未覆盖真实minigame-1，未做本轮原生编译验收、真机或发布。

本轮共5次请求：4次返回透明PNG，第三阶段第一次以429/-10003“参数错误”结束，一次同参数技术重试成功。设计和前两阶段未重画；返回尺寸包括1254×1254，所有成功图片用首次提交前冻结的`alpha-grid-v1`完成转换，无AI修图、无单宠物代码。失败保存在`stage3-attempt-1`，总次数见`generation-history.json`；流水线缓存恢复只统计活动4阶段，最终汇总额外含归档请求，不能漏掉失败成本来源。多候选全部保留并选择首个通过Alpha/分离检查的结果。

151项普通测试、29项素材测试、资源/check/build、微信导出检查通过。375/390/1280浏览器验证最新首位且免费、200积分规则、独立存档与配色、零余额无限喂养、27姿势和6/11级进化；15级程序效果通过。实际四张原图禁止网络重切，图集哈希完全相同；7个成品PNG缓存恢复哈希也相同。已查看动作图集、手机/桌面、喂食与升级截图，结果页10图、9姿势、3种查看底色均无错误。

保留`trial:true`：模型没有充分执行原创替代分支，设定仍是输入插画的衍生形象；第二阶段脸型变圆、红围巾变为蓝绿色。已在`art-review.json`记录，不宣称零美术差异或独立原创。服务端、支付、云存档仍未部署。

## 2026-09-17 最新：三阶段火系伙伴完整试装已完成

最终目录 `artifacts/ember-playable-20260917`，ID `custom-ember-guided-01`，名“星焰伙伴”。三阶段27姿势、透明图集、头像、像素配色与配置已完成；复用共享播放器，无宠物专属代码或AI后处理。`candidate/`是完整素材包，`wechat-test/`包含8原角色＋两个既有小柴＋新伙伴，总16.32MiB、主包236KiB。预览服务8783：`/review/`查看全部动作与实际提示词，`/wechat-test/preview.html`直接玩无限积分版。微信CLI成功打开该独立工程，未覆盖真实minigame-1、未做真机或发布。

用户随后明确：外放技能效果贴框不应整张判废，只要角色本体完整；可适当扩大绘制框。现行 `guided-windows-v7` 将动作内框扩大到416×416，实际裁切始终是外层固定512格。辅助线误差与边缘相连的已知灰底通过通用规则清理，不盲裁内框边缘；稍跨内框的前景像素原位保留，贴内框只标记人工检查。全格去底、边界验收、每阶段共同比例与最终透明留白保持。详见 [GUIDED-PET-LAYOUT.md](GUIDED-PET-LAYOUT.md)。最终recipe把已有设计和前两阶段原图作为冻结输入，第三阶段提交前锁定现行代码；旧失败任务和规则记录全部保留。

默认 `pet:generate` 已切换完整模板流程；旧直接生图保留为 `pet:generate:direct`。支持1—3张用户图、暂停检查、已有设计/动作复用、异步恢复。302可能在n=1时返回多张中间候选，下载器全部保存并固定选最后返回项，再走同一检查，不为响应形状重复付费。本轮共8次POST：6次返回图片、2次429；其中1次返回两张候选。最终目录自己的1次请求仅指第三阶段，其余来源和失败记录见 `generation-history.json`，不可误称整套只调用1次。

150项普通测试、25项素材测试通过；资源/check/build、微信导出检查通过。375/390/1280宽度验证新宠物首位且免费、199/200兑换边界、独立成长和重开配色、无限积分零余额喂养、27姿势、Lv.6/11全屏进化、正式存档隔离；15级程序效果和嵌入食物通过。结果页10张图、9种姿势切换无缺图。已经查看原图、透明图集、手机/桌面、喂食和升级截图。

保留trial：初始形态仍明显接近用户输入插画，不能称为独立原创；局部火焰边缘/封闭区域仍有少量抠图绿边或颗粒。`art-review.json`记录仅接受本机试装以及对应PNG哈希。格式规则不能自动证明任何输入的美术、身体或权利合格。后台生成服务、支付、云存档未部署。

## 2026-09-16 最新：空白格线模板实验

用户提出预画分界线再让模型填图。新增独立 `scripts/pet-guided-study.cjs`（`npm run pet:layout-study`）、`pet-layout-guide.cjs`、`pet-guided-prompts.cjs`，详见 [GUIDED-PET-LAYOUT.md](GUIDED-PET-LAYOUT.md)。设计输入顺序为模板/用户图/画风，动作模板为模板/定稿；灰色辅助区和线条保留给固定程序去除。规则与代码哈希在本次请求前冻结，生产生成器未切换。

实际仅1次302.AI请求，目录 `artifacts/guided-layout-study-v1`。新图保留模板，主体都位于各自白框内；辅助区检查通过，但框内留白不够，第一格底部31px未达到32px，检查停止。只读诊断三阶段最小白边约31/14/4px，结果是 `layout-rejected`。未修改门槛放行、未制作动作图或导入游戏。设计可预览；下一步应区分明确可绘制区与程序补上的最终留白，先写好新规则再生成验证。3项离线测试通过，覆盖模板规范、错误拒绝及动作与原转换器兼容。不能把单次更整齐视为无人值守成功。

## 2026-09-16 重新生成：固定原创模板实测

用户要求重新生图，已新建 `artifacts/fantasy-original-v1`，使用 `generic-original-v1` 提示词与预先锁定的转换规则；未复用之前不完整的角色定稿或动作图。输入仍为用户上传图和固定美术参考，使用已配置的302.AI Sunburst。首个设计请求以429／-10003“参数错误”结束，未返回图片，也未给出审核原因；记录保存在 `design-upstream-failed-1`。同输入、同提示词的一次技术重试返回3072×1024图片，但人工查看发现初始形态仍沿用输入身份，因此未制作动作、未导入游戏，目录状态为 `design-rejected-by-visual-review`。

此实测说明不能只让模型自行识别版权角色。现将通用模板升级为 `generic-original-v2`：自然主体照片保留可用特征；插画、虚拟角色和不确定输入默认只取一至两个宽泛主题，从第一阶段构思独立身份。这条规则不包含当前样例的名称、颜色或器官描述，也适用于用户自有插画（暂不提供授权分支）。动作提示和所有转换几何门槛保持。新任务 `artifacts/fantasy-original-v2` 首张2172×724设计已明显改变脸部与身体结构，但固定三等分时第三阶段前爪被截断，保存在 `design-layout-rejected-1`，未继续动作。同提示词重新生成的最新图在 `design/image-1.png`，为深色晶甲、火冠的四足三阶段形象；仍存在第二阶段前足与侧鳍越过左格线、第三阶段上方留白不足。它可用于设计预览，但不能称为格式合格或直接入库，状态为 `design-preview-only-layout-rejected`。本轮总计4次302请求、3张设计输出、1次429，0次动作请求，未导入新宠物或制作新游戏测试包；不继续盲目追加请求。更新后的单图/多图同文案、规则冻结、缓存离线转换及顺序请求3项测试通过。下一步需要先提高设计排版的稳定性，并在任何新生成前冻结通用规则，不能为现有图片临时挪动切线。

## 2026-09-16 最新：固定通用提示词与原创替代

用户要求固定提示词适用于未知的用户图片：通常保留相似度；不适合直接复现时由模型只保留宽泛题材，重新构思原创伙伴，不必与原图完全相像。已写入 `scripts/pet-gpt-prompts.cjs`，提示词版本 `generic-original-v1`，图片合同仍为fantasy-grid-v3。设计时决定常规参考/原创替代，动作阶段只沿用定稿，避免不同格子换成不同身份；相似度、配色与配饰保留要求均服从原创选择。文本未写入本次样例的角色名或外貌。

单张及一至三张输入均整理为图1，图2是固定画风参考，设计提示词逐字相同；动作仅按固定阶段编号/底色参数填充。实际全文导出到 `docs/prompts/gpt-pet-design.txt` 与三个gpt-pet-stage文本；生成任务记录promptVersion与规则哈希。说明见 [FANTASY-PET-CONTRACT.md](FANTASY-PET-CONTRACT.md)。模型不能保证识别版权或给出商用安全结论；这是生成前的原创设计约束，不是审核拦截后的自动重试或换模型绕过逻辑。

本轮只修改固定提示词、任务记录与说明，未提交新生图请求、未导入未完成样例。旧小火龙衍生任务仍不完整；提示词哈希已改变，不能用现行代码恢复旧任务或修改其记录。后续新的原创设计使用新任务目录，保留旧失败记录。

验证：9项相关离线检查通过，覆盖单图/多图真实请求准备后的提示词字节一致、锁定规则、顺序生成、已有图片复用、错误不重复提交、尺寸标准化与配色。此验证不代表模型实际完成过新模板的原创分支，也不代表版权审核通过。

## 2026-09-16 历史续作：通用奇幻伙伴 v3（样例未完成）

用户要求放开进食方式、强化奇幻形态与动态动作、按角色像素主色搭配背景特效、生成伙伴最新排第一且免费拥有、普通内置伙伴改为200积分。已实现；首次免费初始领养、旧存档与喂养成长规则保留。默认 pet:generate 为302.AI Sunburst异步任务；设计只参考暹罗猫画风，动作只传对应阶段定稿，不传猫的动作或食物。

用户明确允许通用同宽高比标准化。规范见 [FANTASY-PET-CONTRACT.md](FANTASY-PET-CONTRACT.md)，CONTRACT为fantasy-grid-v3：原图比例和最低分辨率先验收，再等比到标准尺寸，固定切片/去底/同阶段统一缩放/像素主色配色。未放宽去底和边界门槛，无AI后处理。新提示词要求每格96px空白来降低实际模型画满格子的概率；固定转换仍按原硬门槛拒绝不合格图。

最新任务 artifacts/fantasy-ember-final，ID custom-ember-v2-01，名“焰尾伙伴”。设计复用 artifacts/fantasy-ember-v2/design-format-failed-1/image-1.png（2172×724→3072×1024）。阶段2已通过固定转换。第一阶段302连续三次429，用户允许改用内置GPT后，内置返回output moderation_blocked（other，未说明具体原因）；无输出图片，记录在builtin-stage1-result.json。第三阶段两次因边界不足拒绝，最新图第8格顶部只有1px，保留原图和rejection.json。用户随后提出版权问题，尚未批准制作未完成测试版；这套小火龙衍生素材未导入新游戏，需确定新的原创素材方向。来源与请求次数记录在design/source.json、attempts.json。旧v2的三次设计请求为两次上游失败、一次正确比例设计；旧v3三个动作请求为第一阶段上游失败、第二/三阶段留白不足，被冻结转换器拒绝。旧请求均保留，不再查询失败任务或重复POST。

上述样例停止在未完成状态。现行提示词已更新，不能对artifacts/fantasy-ember-final套用新提示词继续请求；新设计需新目录。旧测试目录和catalog仅为准备文件，未产生包含该样例的wechat-test。

149项普通测试、17项离线素材测试、资源/check/build通过；网页8角色×3阶段×手机桌面和微信基础336项检查通过。新包待完成后运行wechat-fantasy-smoke（零分免费选择、排序、199/200边界、独立成长和重开配色）、wechat-unlimited-smoke（27姿势与进化）、wechat-procedural-smoke。尚未将新例完成或微信真机发布验收。

## 2026-09-16 当前：Sunburst 小柴完整素材＋独立测试版

用户确认302.AI `gpt-image-2.5-sunburst` 三阶段设计后，继续生成三阶段九宫格动作，本轮实际3次请求，均首次输出成功。目录 `artifacts/shiba-sunburst-kit`，新ID `custom-shiba-sunburst-01`，名“小柴·日曜”。设计复用上轮图片；动作输入为对应阶段设计＋暹罗猫动作排版，保留小柴物种与配饰。固定裁切、去绿底、等比定位、头像与配置生成在禁止网络时完成，不调用AI，也不增加角色专属代码。背景、特效和食物图标继续来自程序预设。

新增 `scripts/api302-image.cjs` 保存异步任务号，恢复时只查询与下载，不重复计费请求；不确定POST需要核对供应商记录。凭据仅从本机忽略环境读取，不在微信包。用法见 [302-PET-ART.md](302-PET-ART.md)，默认 `pet:generate` 仍保留Seedream路径。

独立 `wechat-test` 包含8原有角色＋旧小柴＋新日曜小柴，默认无限积分，新小柴自动选中，两个本地伙伴独立保存；主包231KiB，总计14.94MiB。未覆盖真实minigame-1。新预览端口8780，完整结果 `/review/`，游戏 `/wechat-test/preview.html`。微信CLI成功打开该独立工程，这不代表完成真机或发布验收。

147项普通测试、11项离线素材测试、资源/check和微信导出检查通过。375/390/1280浏览器检查了两只本地宠物、独立成长和重复领取、27姿势、无限积分、两次全屏进化及正式存档隔离；15级程序特效及喂食检查通过，无背景/特效/食物PNG请求。已查看三阶段、喂食和升级截图。第二、三阶段动物本体差异偏细，技能为关键姿势＋程序特效，仍保留试装与人工美术判断标记。没有部署订单后端、微信真机、上传、审核或发布。

## 2026-09-16 最新设计修正：暹罗猫仅提供美术风格

用户认为上一版服饰与暹罗猫过于相似，要求三阶段设计自由发挥，不参照猫的形态或进化幅度。已修改 `pet-template-prompts.cjs` 的设计提示词：参考仅限笔触、材质、光照，主体性格、能力、轮廓、服饰和成长路线独立构思；保持三等宽列输出格式，删除参考体型占比要求。此次仅调用一次Pro，生成 `artifacts/shiba-siamese-template/design-style-only-v2/image-1.png`（3072×1024），实际提示词与两张输入参考在同目录。新图保留紫色项圈和圆形挂牌，去掉上一版猫的披风与肩饰；第三阶段主要通过体格、毛发和气质进化。未生成新动作、未更换游戏中的上一套素材；旧设计和任务选择完整保留。提示词现有检查通过。

## 2026-09-16 最新：暹罗猫角色参考＋程序预设

用户进一步确定：能由程序绘制的背景、特效、食物图标不再生图；角色与动作逐项参考暹罗猫，统一画风和裁切布局。当前默认入口 `pet:generate` 已改为 `pet-template-pipeline.cjs`：一张三阶段设定、三张九宫格动作，正常共四次 Pro 请求。照片仍负责主体身份，模板负责风格、姿势与位置，不能把模板服装误称为照片原有特征。

`wechat/runtime/pet-presets.js` 提供三类背景、三类特效、三套配色、三种食物图标。按唯一宠物 ID 生成稳定种子并将选择写入 `pet.json.procedural`；新 ID 可得到不同组合，刷新不重新随机。程序绘制15级累积特效及进化特效。头像从动作图裁出；进食姿势已有食盆，不再重复叠加食物贴图。新图集使用 `template-v1`、512格和448足底，旧素材包仍兼容。尺寸、裁切、抠底、对齐与装配全部为确定性代码，无视觉模型和AI修图。

本次小柴在 `artifacts/shiba-siamese-template`，ID `custom-shiba-template-01`。联调初期转换模块未就绪导致第一阶段多请求一次，实际五次角色生图，已将模块检查前移；恢复任务保持已选的 `stage1-attempt-2`。阻断网络重新装配通过。详细格式见 [PROCEDURAL-PET-ART.md](PROCEDURAL-PET-ART.md)。

独立无限积分工程在该目录的 `wechat-test`，8原角色＋小柴，主包约229KiB、总计13.84MiB，未覆盖真实 minigame-1。新预览端口8779，游戏 `/wechat-test/preview.html`，参考图与可交互预设展示 `/review/`。145项普通测试、11项离线素材测试、资源清单与微信导出检查通过；375/390/1280浏览器验证以及15级程序特效、喂食、保存稳定性、无背景/特效/食物图片下载检查通过。照片相似度与美术仍需人工评价，保留试装标记；本轮未做真机、上传、审核或发布。用户上传生成服务端仍未部署。

## 2026-09-16 历史：Pro自主判断主体＋小柴无限积分测试

用户确定默认 Pro 无其他角色参考。通用提示词让模型判断照片主体类型、成熟程度和对应阶段，适用于动物、人物、物品，不再假定输入是幼年。仍按可爱→灵动→帅气进化，保留辨识特征与标志配饰；后两阶段自由发挥。默认不调用视觉模型，生成完成后全部为确定性切图、去底、对齐、图集与JSON装配。

新照片完整结果在 `artifacts/photo-pet-pro-20260916`，ID `custom-photo-shiba-01`、名“小柴”。Pro七次生图成功，没有生成重试、视觉模型或AI修图。三个动作图集共27姿势，另有头像、食物、场景、四类特效。`candidate/quality.json.conversion` 表示技术转换通过、AI调用数0；仍保留美术待检查标记，不能把格式检查称为自动审美验收。已阻断网络重跑实际缓存任务，输出可重复装配。

独立工程在 `artifacts/photo-pet-pro-20260916/wechat-test`，通过 `--test --test-default` 打开即无限积分，包含原8角色＋新小柴，主包224KiB、总计14.79MiB。真实 minigame-1 没有被这次测试构建覆盖。微信CLI已打开独立工程，浏览器预览为 `http://127.0.0.1:8778/wechat-test/preview.html`，完整图库为同端口 `/review/`。本机服务需保持运行；静态结果仍可从对应文件查看。

无限积分是测试控制器的虚拟额度：保留JSON安全数值，调用共享交易后恢复原余额，原经济与成长逻辑不变。测试时自动解锁新增本地宠物，旧进度保留；正式构建及release环境不开放测试，`test=0` 可在独立测试构建核对正式流程。141项普通测试、9项离线素材测试、asset/check/build和导出检查通过；375/390/1280浏览器实测新宠物、27姿势、零积分喂养、存档、两次全屏进化、正式规则隔离通过。未完成本轮微信真机、上传、审核或发布。

## 2026-09-16 当前继续点：Seedream 半写实照片伙伴

用户最新要求恢复豆包生图，尝试 Lite / Pro × 无参考 / 暹罗猫参考四版三阶段设计，再制作完整素材。已成功得到四版，位于 `artifacts/dog-evolution-study`。Pro 不支持 `sequential_image_generation`，客户端已按型号省略该参数，未改用其他模型。LaoZhang 三次429是已结束的历史实验，不再恢复。

制作源为 `scripts/pet-design-prompts.cjs` 和 `scripts/seedream-pet-pipeline.cjs`。不再强制孙悟空灰色轮廓或比熊动作参考，三阶段为半写实可爱→灵动→帅气。`--reference none|siamese`、`--design-image`、`--model` 为固定选项，后续动物仅替换照片。锁定设计后每阶段动作只传本阶段设计。本轮候选使用 recipe v3；读完官方文档后通用提示词已精简至300汉字内，后续 recipe v4 使用新目录，禁止与旧请求混用。密钥仅位于被忽略的 `.env.local`，禁止显示或提交。

本轮完整候选分别在 `artifacts/dog-kit-lite`（Lite + 暹罗猫设定）、`artifacts/dog-kit-pro`（Pro + 无参考设定）。两套完整素材均已安装到 minigame-1，保留原小狗，共3只本地自定义伙伴。新增伙伴分别为 custom-scarf-lite / custom-scarf-pro；每只独立资源分包，主包227KiB，总计18.83MiB。139项普通测试、8项离线素材测试、两新宠物及三本地宠物浏览器流程通过。图库为 artifacts/dog-evolution-study/review/index.html。准确验收状态以各目录 `pipeline-status.json`、`candidate/quality.json` 和新浏览器记录为准，不能只凭生成成功宣称美术合格。

模型实际输出的蓝底有渐变和色偏，原单像素抠图过严，已增加仅从画面外边缘连通区域提取背景的通用规则，仍保留透明留白、非空及截断检查。客户端按图片包注册，不需要每只动物生成代码；9个姿势由共享播放器切换和位移，不等同连续武打/骨骼动画。真实照片上传后台、支付、资源托管和云成长存档仍未部署。

## 自定义宠物客户端续作

已实现标准图片包驱动的微信自定义伙伴，支持多次照片草稿、多任务、多个独立成长伙伴及分页；服务端尚未提供，当前导出配置关闭网络生成。新增接口、打包命令与后续服务端要求见 [CUSTOM-PETS.md](CUSTOM-PETS.md)。客户端已更新到用户的 minigame-1，128项单元测试及新旧浏览器流程通过；具体边界见 VALIDATION.md。后续无需每只宠物定制代码，需按规范产出透明动作图集并实现文档中的服务。

## 微信版本增补

本次已按用户提供的 `minigame-1`（微信小游戏类型）开发。可维护源文件位于 `wechat/`，使用 `scripts/build-wechat.cjs` 从现有 `game/` 和资源清单导出完整工程。微信存档独立；正式构建不启用测试入口。参见 `wechat/README.md` 和 `docs/WECHAT-MIGRATION.md` 顶部的当前实现记录。以下“未开始微信工程”的内容是此前整理阶段的历史记录。

## 当前完成状态

网页主体已完成，保留幼儿园及小学口算、8角色养成、自由初始领养与积分兑换、随机动作、三阶段/15级视觉、全屏升级、手机与电脑布局。最新修改是孙悟空「可爱→灵动→帅气」完整54姿势、哪吒半写实武打，以及喂养提示移到宠物脚下。

本次工作目标是整理、交接、上传 main，未开始微信工程实现。后续应以此目录为唯一源码项目；旧对话和旧项目里的 output、zip、临时发布仓库不再是依赖。正式入口 `/`；测试入口 `/?test=1`。

## 继续开发从哪里开始

先读 DEVELOPMENT.md 了解纯逻辑和渲染分层，再读 CHARACTERS.md 和 WECHAT-MIGRATION.md。运行 npm test、npm run check、npm run build；如需 UI 检查运行浏览器脚本。不要用旧版构建脚本覆盖新图集。

当前没有：微信 appid/工程、用户登录、服务器、云同步、音频、骨骼动画或支付。手机端实际视觉还应持续在微信内置浏览器与真机复核，桌面 Chrome 仿真不能代替全部设备。

## 整理范围

- 保留：当前入口和游戏代码，8角色的最终图集/头像/独立姿势/食物 PNG，必要角色数据和特效，现行单元测试。
- 新增：README、AGENTS、完整开发/角色/微信迁移说明、资产哈希与帧清单、零依赖启动/检查/打包脚本、便携浏览器验收、GitHub 检查流程。
- 从新目录排除：原始生成底图、提示词中间迭代、旧角色备份、重复 WebP、zip、旧根目录原型、studio/preview/embed演示、过时 manifest/README/历史进度、旧 qa 截图/GIF/报告、固定旧绝对路径的脚本。
- 没有删除上级旧工作区。整理后的当前 Git 工作树不含这些过程文件，历史提交仍按 Git 语义保留；没有改写整个仓库历史来清理旧 blob。

## 版本与存档注意

代码不再依赖 `wukong-game/` 这个外层路径；仓库根 index.html 直接部署。继续使用相同 GitHub Pages origin 可保持浏览器原存档，源代码文件复制不包含存档本身。本地从旧端口换到 8766 会是另一份存储空间。

图集和脚本有 URL 版本参数。不要把测试版默认打开行为硬编码到正式入口。网页测试版起始 10,000 分，可手动增加，不是不会扣减的无限值。

## 当前验收基线

整理前最新 110 项单元测试全部通过；孙悟空54帧透明边界、三阶段动作、手机/桌面全屏升级已检查。新目录应再次运行相同测试、清单校验及独立浏览器验收，完成记录见 VALIDATION.md。

## 2026-09-16 本地小狗试装

微信导出支持 `--local-pets` 目录，客户端从制作页“加入本地伙伴”领取，多个编号独立存档。第四版围巾小狗的裁切试装在忽略目录 artifacts/scarf-dog-trial；构建需显式 `--allow-trial-pets`。白底、半身、睡眠动作和合成特效不符合成品标准，详见 CUSTOM-PETS.md。尚无真实生成后端；默认构建仍只有原有 8 位伙伴。
