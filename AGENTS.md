# 项目交接约定

这是算算萌宠的独立、可发布源码仓库。开始工作先读 README.md、docs/DEVELOPMENT.md、docs/HANDOFF.md；涉及微信版本再读 docs/WECHAT-MIGRATION.md。

## 已确认的产品约束

- 当前 8 个角色，精卫已退役，不恢复她的入口或素材；保留旧存档 retiredPets 迁移逻辑。
- 正式版首次免费任选一个初始伙伴，其余每个 200 积分。不是答题数量达标自动解锁。不要重复赠送初始伙伴或清空旧存档。生成的自定义伙伴免费拥有、最新的排在列表最前面。
- 喂养和做题是两个独立操作，手机两列紧凑排列。喂食 500ms，成功提示放在宠物脚下、成长进度上方，不遮挡操作按钮，不拦截点击。
- 正确一题 +10 分；一份食物 20 分换 20 成长值。Lv.6/11/15 累计需要 300/1000/1500 成长值；Lv.15 后每级 300，只有外观停在 15 级。
- 孙悟空最新版本为可爱→灵动→帅气；进化后不使用幼态卖萌动作。哪吒为半写实武打形象，成年帅气。玉兔保留原卡通版。布偶猫幼年奶凶傲娇，成年较瘦、友好；柯基穿警服；萨摩耶戴眼镜。
- 测试入口 ?test=1，正式入口不带这个参数；两套本地存档隔离。调试工具不等同于真正的安全隔离或反作弊。

## 工程约束

- 本目录是项目根，index.html 可直接运行。不能引用上级目录、旧的 output/、临时目录或某台电脑的绝对路径。
- 不更改积分、成长、初始宠物等规则来配合视觉修改，除非用户要求。数据层与角色渲染层保持分离。
- 新角色通过 game/ 中适配配置注册，并更新 core.js 的已知兑换规则及必要测试；精灵图布局必须匹配播放器。参考 docs/CHARACTERS.md。
- 修改脚本或图片后更新 index.html 的对应查询版本和播放器的素材版本，防止缓存旧图。
- 修改素材或动作/阶段数据后运行 npm run assets，再运行 npm run check。asset-inventory.json 是当前完整清单；旧项目的 manifest、设计说明已移除，不要依赖它们。
- 不提交 node_modules、dist、截图、生成过程、备份、旧图集、下载缓存、凭据或真实用户存档。最终单帧 PNG 是可编辑成品，不是过程垃圾。
- npm test / npm run check / npm run build 无安装依赖。播放器或界面改动应额外用浏览器检查对应角色、手机与桌面、喂食和全屏升级。
- 这是有独立 .git 的仓库；仅在本目录操作 Git。推送按当前用户授权执行，普通维护优先正常推送，避免强推覆盖他人提交。

## 后续微信工作

已根据用户提供的 minigame-1 开发微信小游戏，类型为 game。适配源在 wechat/，通过 scripts/build-wechat.cjs 导出，复用 game/ 的纯逻辑；不能将 DOM、Shadow DOM 和 dialog 直接当作微信组件。微信导出额外需要 sharp，原网页测试/检查/打包仍无依赖。使用 npm run check:wechat -- <导出目录> 检查入口、图集与核心逻辑哈希。保留目标工程已有 AppID 和个人配置；编译、真机、审核与发布状态必须分别说明，不要把浏览器模拟验收描述为已完成微信发布。迁移文档列出了已知差异和验收标准。
